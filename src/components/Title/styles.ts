import theme from '../../theme/base';

export const createStyles = () => ({
  title: {
    color: theme.colors.brandPrimary,
    marginHorizontal: theme.spacing.marginHorizontal * 3,
    marginVertical: theme.spacing.marginVertical * 1.5,
  },
});
